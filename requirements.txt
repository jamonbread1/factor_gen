# GM3 data access is supplied by the 掘金 terminal environment.
# PyTorch wheels bundle the CUDA runtime; nvidia-smi CUDA 13.1 is the driver capability.
torch>=2.4
pandas>=2.0
numpy>=1.26
scipy>=1.11
tqdm>=4.66
loguru>=0.7
